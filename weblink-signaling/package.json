{
  "name": "weblink-signaling",
  "version": "1.0.0",
  "private": true,
  "description": "Sovereign WebRTC signaling relay — Cloudflare Worker + Durable Objects",
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "deploy:dev": "wrangler deploy --env dev",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}
